-- Run this command to export your database:
-- pg_dump -U postgres -h localhost expenseflow > database-export.sql

-- Your friend can import with:
-- psql -U postgres -h localhost expenseflow < database-export.sql